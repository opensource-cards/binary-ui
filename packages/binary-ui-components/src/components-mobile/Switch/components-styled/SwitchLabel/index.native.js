import { FONT_SET_BOLD_CSS } from 'binary-ui-styles';
import styled from 'styled-components/native';

export default styled.Text`
  ${FONT_SET_BOLD_CSS}
  flex: 8;
  padding-left: 10;
  padding-right: 10;
`;
